export type Experience = {
  period: string;
  title: string;
  company: string;
  description: string;
};

export type Skills = {
  frontend: string;
  backend: string;
  tools: string;
};

export type ContactFormData = {
  name: string;
  email: string;
  message: string;
};
